from django.apps import AppConfig

class TifToPickerConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.tif_to_picker'  # Make sure this matches your directory structure